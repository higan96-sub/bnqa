<ul class="visible-lg nav nav-tabs nav-justified following" style="border-bottom: 1px solid #ddd">
    <li class="active">
        <a href="#">{% if followingItems != null %}フォロー中{% else %}Amazonランキング{% endif %}</a>
    </li>
</ul>
<div class="visible-lg">
    <div class="following-sidebar-wrapper">
        <div class="following-sidebar">
            <div class="sidebar-body">
                {% for item in followingItems %}
                    {% include "AcmeBnqaBundle:Sidebar:_home_item_unit.html.twig" with{'item':item} %}
                {% else %}
                    <div class="sidebar-unit">
                        この位置にフォローしたアイテムが表示されます。<br>
                        <strong>興味のあるアイテムをフォローしてみましょう。</strong><br>
                    </div>
                    {% render controller("AcmeBnqaBundle:Item:topSellers") %}
                {% endfor %}
            </div>
            {% if followingItems != null %}
                <div class="load-items">
                    アイテムを更に読み込む
                </div>
            {% endif %}
        </div>
    </div>
</div>